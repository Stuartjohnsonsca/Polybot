# Polymarket Gamma API base (public, no key required for read).
GAMMA_API_BASE=https://gamma-api.polymarket.com

# Postgres connection for basket persistence. On Vercel, connecting Neon
# (Storage → Create → Neon Postgres) auto-injects DATABASE_URL.
# DATABASE_URL=postgres://user:pass@host/db?sslmode=require
